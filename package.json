{
  "name": "crypto-flow-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "pnpm run --workspace=client dev",
    "build": "pnpm run --workspace=client build",
    "lint": "pnpm run --workspaces lint",
    "type-check": "pnpm run --workspaces type-check"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
